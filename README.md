Десктопное приложение для управления базой данных банковских вкладчиков.
Разработано в рамках **Лабораторной работы №4** (Портирование консольного приложения в Windows Forms).

## 📋 Описание проекта

Программа позволяет вести учет вкладчиков, сохранять данные в файл и выполнять базовые операции манипулирования данными. Реализована строгая архитектура с разделением логики и интерфейса, а также защита от некорректного ввода данных.

## ✨ Функционал

### Основные возможности:
* **Добавление:** Ввод имени и суммы вклада через модальное окно.
* **Редактирование:** Изменение данных существующего вкладчика (форма открывается с текущими данными).
* **Удаление:** Удаление выделенной записи из таблицы.
* **Сортировка:** Автоматическая сортировка списка по убыванию суммы вклада.
* **Файловые операции:**
    * Сохранение базы данных в текстовый файл (`.txt`).
    * Загрузка базы данных из файла.

### Технические особенности:
* **Валидация данных:**
    * Защита от ввода букв в поле суммы.
    * Проверка на пустые строки.
    * Ограничение суммы вклада (от 0 до 100 000).
* **Интерфейс:**
    * Использование таблицы `DataGridView` в режиме `FullRowSelect`.
    * Перегрузка конструкторов формы ввода для изменения внешнего вида (желтый фон при редактировании).
    * Защита таблицы от прямого редактирования (ReadOnly).

## 🛠 Технологии

* **Язык:** C++ (C++/CLI)
* **Платформа:** .NET Framework (Windows Forms)
* **Среда разработки:** Visual Studio
* **Хранение данных:** `std::vector` (STL)

## 🏗 Архитектура (UML)

Структура классов проекта:

```mermaid
classDiagram
    class RegularDepositor {
        -string fullName
        -double depositAmount
        +RegularDepositor()
        +getName() string
        +getAmount() double
        +setName(string)
        +setAmount(double)
    }

    class InputForm {
        +String EnteredName
        +double EnteredAmount
        +InputForm()
        +InputForm(currentName, currentAmount)
    }

    class MainForm {
        -vector~RegularDepositor~* db
        -DataGridView dataGridView1
        +MainForm()
        -UpdateGrid()
        -save_load_Click()
    }

    MainForm *-- RegularDepositor : "Хранит список"
    MainForm ..> InputForm : "Создает"
    InputForm ..> RegularDepositor : "Редактирует"
